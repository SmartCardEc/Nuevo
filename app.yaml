runtime: python
env: flex
entrypoint: gunicorn -b :$PORT main:app

runtime_config:
  python_version: 3.10.8

manual_scaling:
  instances: 1

env_variables:
  FLASK_APP: main
  FLASK_ENV: development

